---
title: "Get Started with MailValid Email Verification"
sidebarTitle: "Quickstart"
description: "Sign up, create an API key, and verify your first email address with MailValid in under 5 minutes. Includes code examples in cURL, Python, Node.js, and PHP."
---

This guide walks you through everything you need to send your first verification request to MailValid. There are three steps — create an account, grab your API key, and make a request — and the whole process takes under 5 minutes.

<Steps>
  <Step title="Create your account">
    Sign up for a free account at [mailvalid.io/register](https://mailvalid.io/register). No credit card is required, and your account comes with **100 free credits** to use right away.
  </Step>

  <Step title="Get your API key">
    After signing in, navigate to **Dashboard → API Keys** and click **Create New Key**. Give the key a descriptive name (for example, `development` or `production`), then copy the generated key. API keys follow the format:

    ```
    mv_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    ```

    <Note>
      Copy your key immediately after creating it — MailValid will not show the full key again after you leave the page.
    </Note>
  </Step>

  <Step title="Verify your first email">
    Send a `POST` request to `https://mailvalid.io/api/v1/verify/single` with your API key in the `X-API-Key` header and the email address in the JSON body.

    <CodeGroup>

    ```bash cURL
    curl -X POST https://mailvalid.io/api/v1/verify/single \
      -H "X-API-Key: mv_live_your_key_here" \
      -H "Content-Type: application/json" \
      -d '{"email": "john@company.com"}'
    ```

    ```python Python
    import requests

    API_KEY = "mv_live_your_key_here"

    response = requests.post(
        "https://mailvalid.io/api/v1/verify/single",
        headers={"X-API-Key": API_KEY},
        json={"email": "john@company.com"}
    )

    result = response.json()
    print(f"Status: {result['result']['status']}")
    print(f"Valid: {result['result']['is_valid']}")
    print(f"Confidence: {result['result']['confidence_score']}")
    print(f"Reason: {result['result']['status_reason']}")
    ```

    ```javascript Node.js
    const API_KEY = "mv_live_your_key_here";

    const response = await fetch(
      "https://mailvalid.io/api/v1/verify/single",
      {
        method: "POST",
        headers: {
          "X-API-Key": API_KEY,
          "Content-Type": "application/json"
        },
        body: JSON.stringify({ email: "john@company.com" })
      }
    );

    const data = await response.json();
    console.log(`Status: ${data.result.status}`);
    console.log(`Valid: ${data.result.is_valid}`);
    console.log(`Confidence: ${data.result.confidence_score}`);
    ```

    ```php PHP
    <?php
    $apiKey = "mv_live_your_key_here";

    $ch = curl_init("https://mailvalid.io/api/v1/verify/single");
    curl_setopt_array($ch, [
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_POST => true,
        CURLOPT_HTTPHEADER => [
            "X-API-Key: " . $apiKey,
            "Content-Type: application/json"
        ],
        CURLOPT_POSTFIELDS => json_encode(["email" => "john@company.com"])
    ]);

    $result = json_decode(curl_exec($ch), true);
    echo "Status: " . $result['result']['status'];
    echo "Valid: " . ($result['result']['is_valid'] ? 'Yes' : 'No');
    echo "Confidence: " . $result['result']['confidence_score'];
    ?>
    ```

    </CodeGroup>
  </Step>
</Steps>

## Example response

A successful verification returns a JSON object with a `result` block containing the full verification output:

```json
{
  "success": true,
  "credits_used": 1,
  "result": {
    "email": "john@company.com",
    "status": "valid",
    "is_valid": true,
    "syntax_valid": true,
    "domain": "company.com",
    "domain_valid": true,
    "has_mx": true,
    "mx_records": [{"priority": 10, "host": "mx1.company.com"}],
    "smtp_checked": true,
    "smtp_response_code": 250,
    "is_disposable": false,
    "is_role_based": false,
    "is_catch_all": false,
    "is_free_provider": false,
    "confidence_score": 95,
    "status_reason": "mailbox_confirmed",
    "provider": "corporate",
    "verification_time_ms": 234,
    "cached": false
  }
}
```

<Note>
  When `cached: true` appears in the response, MailValid returned a recent server-side result for that address. Cached results use **0 credits**.
</Note>

## Next steps

<CardGroup cols={2}>
  <Card title="Bulk Verification" icon="layer-group" href="/guides/bulk-verification">
    Verify up to 10,000 emails in a single asynchronous request — ideal for list cleaning and CRM imports.
  </Card>
  <Card title="Email Statuses" icon="circle-info" href="/concepts/email-statuses">
    Understand every possible `status` and `status_reason` value and how to act on each one.
  </Card>
  <Card title="Webhooks" icon="webhook" href="/guides/webhooks">
    Receive a webhook notification when a bulk job completes instead of polling for results.
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/verify-single">
    Full reference for the single verification endpoint with every request parameter and response field documented.
  </Card>
</CardGroup>
